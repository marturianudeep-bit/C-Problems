#include <stdio.h>

int main() {
    int n, i;

    printf("Enter number of readings: ");
    scanf("%d", &n);

    int arr[n];

    printf("Enter readings:\n");
    for(i = 0; i < n; i++) {
        scanf("%d", &arr[i]);
    }

  
    for(i = 0; i < n; i++) {
        if(arr[i] < 0) {
            arr[i] = 0;
        }
    }

  
    printf("Modified array:\n");
    for(i = 0; i < n; i++) {
        printf("%d ", arr[i]);
    }

    return 0;
}
