 #include<stdio.h>
 int main(){
    int a,b;

    scanf("%d %d",&a,&b);

    int quotient = a;
    int remainder = a;
     
     quotient /= b;
     remainder %= b;

     printf("%d %d",quotient,remainder);

     return 0;
 }
