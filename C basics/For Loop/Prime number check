#include<stdio.h>
int main(){
    int n,prime = 1;
    scanf("%d",&n);

    for(int i = 2; i * i <= n; i++){
        if(n % i == 0){
            prime = 0;
            break;
        }
    }
    if(prime && n > 1)
    printf("Prime");
    else
       printf("NOt Prime");
       return 0;
}
