#include<stdio.h>
int main(){
    int N;
    scanf("%d",&N);

    int successful = 0, cancelled = 0;

    int i = 0,value;

    while(i < N){
        scanf("%d",&value);

        if(value == 1){
            successful++;
        }else if(value == 0){
            cancelled++;
        }
        i++;
        }
        printf("Successful: %d\n",successful);
        printf("Cancelled: %d\n",cancelled);

        if(cancelled > successful){
            printf("status: Risk\n");
        }else{
            printf("Status: safe\n");
        }
        return 0;
        
    }
